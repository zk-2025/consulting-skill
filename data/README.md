# 商业咨询skill 数据目录

此目录存放咨询诊断的存档和报告。

## 目录说明
- `archives/` — 诊断存档（JSON 格式），用户说"保存"时写入
- `reports/` — 诊断报告（Markdown 格式），用户说"出报告"时写入

## 文件命名规则
- 存档：`{userId}_{timestamp}_{name}.json`
- 报告：`{name}_{timestamp}.md`

## 注意
此目录内容为用户数据，不应纳入版本控制。
